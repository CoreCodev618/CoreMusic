import 'package:flutter/material.dart';

/// Paleta oficial de CoreTune — aprobada en el documento de diseño.
/// Fondo verde-teal profundo, acentos coral y ámbar.
class CoreTuneColors {
  CoreTuneColors._();

  // Fondos
  static const Color background = Color(0xFF0E1A1C);
  static const Color backgroundDeep = Color(0xFF0A1314);
  static const Color surface = Color(0xFF152527);
  static const Color surfaceAlt = Color(0xFF16292B);
  static const Color surfaceAlt2 = Color(0xFF1F3033);
  static const Color surfaceCard = Color(0xFF1A2426);

  // Bordes
  static const Color border = Color(0xFF1C2E30);
  static const Color borderStrong = Color(0xFF2A3F41);

  // Acentos
  static const Color coral = Color(0xFFFF6B6B);
  static const Color coralDark = Color(0xFF4A1B0C);
  static const Color amber = Color(0xFFFFD166);
  static const Color amberDark = Color(0xFF412402);

  // Texto
  static const Color textPrimary = Color(0xFFF2EFE9);
  static const Color textSecondary = Color(0xFF9FC2BD);
  static const Color textMuted = Color(0xFF7FA39E);
}
